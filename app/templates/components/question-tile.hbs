<div class="container">
  <h2> Question: "{{question.content}}"</h2>

  <h4>Notes: {{question.notes}}</h4>

  <h3>Answers: </h3>
  <ul>
    {{#each question.answers as |answer|}}
      <li {{action 'best' answer}}>
        {{#if answer.best}}
          🌟
        {{/if}}
        {{answer.answerContent}}{{#link-to 'answer' answer.id}}Mode details{{/link-to}}
      </li>
    {{/each}}
  </ul>

  <br>
  <button class='delete-button'{{action 'destroyQuestion' question}}>Delete Question!</button>
</div>
