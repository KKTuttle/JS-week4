import { popularityQuestion } from 'message-board/helpers/popularity-question';
import { module, test } from 'qunit';

module('Unit | Helper | popularity question');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = popularityQuestion([42]);
  assert.ok(result);
});
